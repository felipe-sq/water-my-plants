node_modules/
dist/
build/

# Every .env variant stays out of git, except the checked-in template.
# The negation must come last, or a later broad pattern re-ignores it.
.env*
!.env.example

.vercel
.DS_Store
*.log
npm-debug.log*
coverage/
